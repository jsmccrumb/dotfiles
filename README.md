# dotfiles
my configurations when i get around organizing them

nvim `ln -s "$(pwd)/nvim/" ~/.config/`
tmux `ln -s "$(pwd)/tmux/.tmux.conf" ~/` and download the plugins referenced (doesn't use tpm currently)
